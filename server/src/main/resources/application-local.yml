storage:
  directory: ./storage
spring:
  datasource:
    url: jdbc:postgresql://localhost:5450/learnlanguage
    username: postgres
    password: postgres
  cloud:
    azure:
      active-directory:
        credential:
          client-secret: ${api-client-secret}
management:
  server:
    port: 8152
